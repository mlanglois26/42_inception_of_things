apiVersion: apps/v1
kind: Deployment
metadata:
  name: app1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app1
  template:
    metadata:
      labels:
        app: app1
    spec:
      containers:
      - name: app1
        image: nginx:latest
        ports:
        - containerPort: 80 # par défaut ne pas changer
        volumeMounts:
        - name: html-1
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html-1
        configMap:
          name: app1-index
